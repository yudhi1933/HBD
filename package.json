{
  "devDependencies": {
    "tailwindcss": "^3.4.14"
  
  },
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "tail": "npx tailwindcss -i ./src/input.css -o ./public/css/style.css --watch && exit 0"
  }
}
